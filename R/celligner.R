run_celligner <- function(x){
  # load_data

  # make table with ENSG, HGNC, SD, mean for each gene in the tumour and CL data
  #    is later added to `DE_genes`

  # combine annotation and add tumour / CL designation

  # create 2 seuarat objects

  # cluster tumour and CL separately cluster_data()
}
